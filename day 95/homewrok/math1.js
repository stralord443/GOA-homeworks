function min(a, b) {
    return a < b ? a : b
}
function max(a, b) {
    return a > b ? a : b
}
function average(a, b) {
    return (a + b) / 2
}
export const utils = {
    min,
    max,
    average
}